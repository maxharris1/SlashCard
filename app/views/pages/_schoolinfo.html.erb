<div class="card" style="width:200px">
  <div class="card-body">
    <h2 class="card-title info_logo"><%=v.name%></h2>
    <h5><%= link_to school_path(v), class: "btn btn-success btn-xs" do %>
      <i class="glyphicon glyphicon-eye-open"></i> View
    <% end %></h5>
  </div>
</div>
